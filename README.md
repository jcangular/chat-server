# Servidor Chat con Socket y API Rest

Para reconstruir las dependencias y módulos de node:

```
npm install
```

Para (sólo) compilar el proyecto:

```
npm run build
```

Para iniciar el servidor:

```
npm run start
```

Para iniciar el servidor en modo de desarrollo:

```
npm run start:dev
```
